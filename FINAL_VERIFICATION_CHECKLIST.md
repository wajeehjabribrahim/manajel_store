# ✅ قائمة التحقق النهائية - Final Verification Checklist

## 📋 قائمة فحص ما قبل النشر

استخدم هذه القائمة بعد إصلاح جميع المشاكل:

---

## المرحلة 1️⃣: فحص الأمان (Security)

### مشاكل الأسعار والمنتجات
- [ ] تحقق من أن جميع الأسعار تُحسب من DB، لا من localStorage
- [ ] تحقق من وجود جميع المنتجات قبل إنشاء الطلب
- [ ] لا يمكن للعملاء تعديل السعر من الطلب
- [ ] الطلبات مع منتجات مفقودة مرفوضة مع رسالة واضحة

```bash
# اختبر:
curl -X POST /api/orders \
  -H "Content-Type: application/json" \
  -d '{"items": [{"id": "fake-id", "price": 1, "quantity": 1}]}' \
# يجب أن يرجع: { error: "Product not found" }
```

### مشاكل الأذونات
- [ ] الطلبات من الضيوف تحتاج guest token صحيح
- [ ] لا يمكن الوصول لطلبات المستخدمين الآخرين
- [ ] الـ admin يمكنه رؤية جميع الطلبات

```bash
# اختبر:
GET /api/orders/some-order-id
# بدون token يجب أن يرجع: Unauthorized
```

### حالات الطلبات
- [ ] جميع الطلبات تبدأ بـ "pending"
- [ ] الحالات الصحيحة: pending, processing, shipped, delivered, cancelled
- [ ] لا توجد حالة "completed"

```typescript
// تحقق من DB:
SELECT DISTINCT status FROM Order;
// يجب أن تظهر: pending, processing, shipped, delivered, cancelled
```

---

## المرحلة 2️⃣: فحص البيانات (Data Integrity)

### معالجة الأسعار
- [ ] الأسعار في السلة تُحدّث من DB عند الدفع
- [ ] تحذير واضح إذا تغير السعر
- [ ] الإجمالي يُحسب بناءً على أسعار DB، لا localStorage

```typescript
// اختبر الكود:
const resolvedPrice = getResolvedPrice(item);
// يجب أن يكون مساوياً لـ DB price
```

### تطابق البيانات
- [ ] جميع حقول المنتج موجودة في كل من DB و Interface
- [ ] nameEn و descriptionEn يُعادان من API
- [ ] الصور موجودة وصحيحة

### حساب الإجمالي
- [ ] الإجمالي = مجموع (سعر × كمية) لكل منتج
- [ ] بدون رسوم شحن مخفية
- [ ] حساب صحيح عند تغيير الكمية

```javascript
// في browser console:
// أضف منتج للسلة، غيّر السعر يدويّاً، ثم اشترِ
// يجب أن يكون السعر من DB وليس localStorage
```

---

## المرحلة 3️⃣: فحص الأداء (Performance)

### localStorage والصور
- [ ] لا توجد صور data URIs ضخمة في localStorage
- [ ] رسالة تحذير واضحة عند امتلاء Storage
- [ ] السلة محفوظة بدون مشاكل

```javascript
// في browser console:
localStorage.getItem('manajel-cart').length
// يجب أن تكون أقل من 500KB
```

### سرعة التحميل
- [ ] /api/products يرجع في أقل من 1 ثانية
- [ ] /api/orders/[id] يرجع في أقل من 500ms
- [ ] لا توجد صور ضخمة (أكبر من 2MB)

```bash
# اختبر السرعة:
time curl https://yourdomain.com/api/products
```

---

## المرحلة 4️⃣: فحص الترجمة (Localization)

### اللغات
- [ ] جميع الواجهات تعمل بالعربية والإنجليزية
- [ ] اتجاه النص صحيح (RTL للعربية)
- [ ] الصور والأيقونات لا تتأثر باللغة

### الترجمات
- [ ] جميع رسائل الخطأ مترجمة
- [ ] بيانات المنتجات موجودة بلغتين
- [ ] التواريخ والأرقام تُعرض بالصيغة الصحيحة

```javascript
// اختبر في console:
localStorage.getItem('manajel-language')
// يجب أن تحتوي على: ar أو en
```

---

## المرحلة 5️⃣: فحص معالجة الأخطاء (Error Handling)

### الأخطاء
- [ ] جميع الأخطاء موثقة في الـ console
- [ ] رسائل الخطأ واضحة للمستخدم
- [ ] لا توجد أخطاء غير معالجة

```typescript
// في Chrome DevTools -> Console:
// قم بإجراء خطأ (مثل الدفع بدون منتجات)
// يجب أن ترى رسالة واضحة، لا undefined أو generic error
```

### الاستثناءات
- [ ] المحاولة مرة أخرى عند فشل الشبكة
- [ ] رسالة واضحة عند انقطاع الإنترنت
- [ ] حفظ البيانات عند الحاجة

---

## المرحلة 6️⃣: فحص الأذونات (Authorization)

### المستخدمون
- [ ] المستخدم العادي يرى فقط طلباته
- [ ] الأدمن يرى جميع الطلبات
- [ ] الضيف لا يمكنه الوصول لطلبات آخرين

### الـ Admin
- [ ] يمكنه تغيير حالة الطلب
- [ ] يمكنه حذف المنتجات
- [ ] يمكنه إضافة فئات
- [ ] لا يمكنه حذف حسابات المستخدمين (اختياري)

```bash
# اختبر:
curl -X PATCH /api/orders/order-id \
  -H "Authorization: Bearer INVALID_TOKEN" \
  -d '{"status": "shipped"}'
# يجب أن يرجع: Unauthorized
```

---

## المرحلة 7️⃣: فحص التحقق من الصحة (Validation)

### البيانات
- [ ] الأسماء: بين 2-100 حرف
- [ ] البريد الإلكتروني: صيغة صحيحة
- [ ] الهاتف: صيغة صحيحة (مع الرمز الدولي)
- [ ] الكمية: بين 1-999

```bash
# اختبر:
curl -X POST /api/auth/register \
  -d '{"email": "invalid-email", ...}'
# يجب أن يرجع: Invalid email address
```

### المنتجات
- [ ] السعر: موجب وأكبر من 0
- [ ] الحجم: صحيح (small, medium, large)
- [ ] الصورة: موجودة أو null

---

## المرحلة 8️⃣: فحص الحالات (Status Flow)

### تدفق الطلب
```
pending ──> processing ──> shipped ──> delivered
   │                                       │
   └─── cancelled (من pending/processing فقط)
```

- [ ] الطلب يبدأ بـ pending
- [ ] يمكن الانتقال من pending إلى processing/cancelled
- [ ] يمكن الانتقال من processing إلى shipped/cancelled
- [ ] لا يمكن الرجوع للحالة السابقة
- [ ] لا يمكن إلغاء طلب متسلم

```javascript
// اختبر:
// 1. أنشئ طلب (يجب أن يكون pending)
// 2. عدّل إلى shipped بدون processing (يجب أن يرجع error)
// 3. عدّل من shipped إلى pending (يجب أن يرجع error)
```

---

## المرحلة 9️⃣: فحص الإشعارات (Notifications)

### الإيميلات
- [ ] تُرسل رسالة إيميل عند إنشاء طلب
- [ ] الأدمن يتلقى إشعاراً
- [ ] العميل يتلقى تأكيداً (اختياري)

```typescript
// تحقق من logs:
console.log("📧 Sending order email to:", email);
console.log("✅ Order email sent successfully");
```

### الرسائل
- [ ] رسائل التواصل تُحفظ في DB
- [ ] الأدمن يتلقى إشعاراً
- [ ] إجابة واضحة للمستخدم

---

## المرحلة 🔟: فحص الإحصائيات (Analytics)

### عدد الطلبات
- [ ] إجمالي الطلبات صحيح
- [ ] الطلبات حسب الحالة صحيحة
- [ ] عدد الطلبات هذا الشهر صحيح

```javascript
// اختبر:
fetch('/api/admin/orders-stats').then(r => r.json()).then(console.log)
// تحقق من الأرقام يدويّاً في DB
```

### الإيرادات
- [ ] إجمالي الإيرادات صحيح
- [ ] إيرادات هذا الشهر صحيحة
- [ ] متوسط قيمة الطلب صحيح

---

## 🧪 اختبارات الـ Edge Cases

### حالات خاصة
- [ ] طلب بكمية 0 (مرفوض)
- [ ] طلب ببريد إلكتروني فارغ (مرفوض)
- [ ] تغيير السعر أثناء العملية
- [ ] حذف منتج من السلة
- [ ] localStorage ممتلئة تماماً

```javascript
// اختبر:
// 1. أضف 999 منتج للسلة
//    أم يجب حد أقصى؟ (100 مثلاً)
// 2. غيّر السعر يدويّاً في localStorage
//    ثم اشترِ وتحقق من السعر المحفوظ
```

---

## 📊 جدول التحقق النهائي

| الفئة | الاختبار | النتيجة | ملاحظات |
|------|---------|--------|---------|
| الأمان | Price Validation | ✅/❌ | |
| الأمان | Authorization | ✅/❌ | |
| الأمان | Product Existence | ✅/❌ | |
| البيانات | Price Match | ✅/❌ | |
| البيانات | Status Consistency | ✅/❌ | |
| البيانات | Total Calculation | ✅/❌ | |
| الأداء | localStorage Size | ✅/❌ | |
| الأداء | API Response Time | ✅/❌ | |
| الترجمة | Arabic Support | ✅/❌ | |
| الترجمة | Error Messages | ✅/❌ | |
| الأذونات | User Isolation | ✅/❌ | |
| الأذونات | Admin Access | ✅/❌ | |
| التحقق | Input Validation | ✅/❌ | |
| الحالات | Order Flow | ✅/❌ | |
| الإشعارات | Email Sending | ✅/❌ | |
| الإحصائيات | Order Count | ✅/❌ | |

---

## ⚡ اختبار الدخان (Smoke Test)

قم بهذا الاختبار قبل كل نشر:

```bash
# 1. التحقق من البناء
npm run build
# يجب أن ينجح بدون أخطاء

# 2. التحقق من الأخطاء
npx tsc --noEmit
# يجب ألا يكون هناك أخطاء TypeScript

# 3. اختبر الـ Lint
npx eslint src/
# يجب ألا يكون هناك أخطاء حرجة

# 4. اختبر الوحدات
npm test
# يجب أن تمر جميع الاختبارات

# 5. اختبر الخادم
npm start
# يجب أن يبدأ بدون أخطاء
```

---

## 🚀 ما قبل النشر النهائي

- [ ] جميع اختبارات Smoke Test تمر
- [ ] التقرير الأخير مراجع من قبل فريق
- [ ] أي مشاكل جديدة موثقة
- [ ] نسخة احتياطية من الإنتاج أُخذت
- [ ] خطة Rollback موجودة
- [ ] الفريق متأهب للمراقبة

---

## 📝 ملاحظات ما بعد النشر

بعد النشر:
- [ ] راقب الأخطاء في الـ Logs
- [ ] تحقق من أداء قاعدة البيانات
- [ ] استمع لتعليقات المستخدمين
- [ ] سجل أي مشاكل جديدة
- [ ] خطط للتحسينات المستقبلية

---

**تم الإنشاء**: فبراير 2026  
**الإصدار**: 1.0  
**الحالة**: ✅ جاهز للاستخدام
